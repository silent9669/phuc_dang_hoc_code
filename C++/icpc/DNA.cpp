#include <bits/stdc++.h>

using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    string sample;
    int n;
    vector<string> strange(n);

    cin >> sample;
    for (int i = 0; i < n; i++)
    {
        cin >> strange[i];
    }

    for (int i = 0; i < n; i++){
        
    }
    

    return 0;
}